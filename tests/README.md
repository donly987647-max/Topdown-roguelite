# Tests

The foundation includes a headless smoke test.

```bash
godot --headless --path . --script res://tests/smoke_test.gd
```

Future stages add deterministic tests for damage, ammunition, inventory placement, room graphs, save migration and run generation.
