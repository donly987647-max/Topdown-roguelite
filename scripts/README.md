# Scripts

Scripts are grouped by gameplay domain, not by arbitrary utility type.

Cross-domain communication must use explicit interfaces or `EventBus`. Global state is restricted to `GameState` and future narrowly scoped services.
